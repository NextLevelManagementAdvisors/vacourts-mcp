# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Local bulk store (virginiacourtdata SQLite — keep local per VA 2018 bulk-data statute)
*.db
*.sqlite
*.sqlite3
*.sqlite-wal
*.sqlite-shm

# Downloaded bulk dumps (re-fetchable via download_anon.py; never commit)
dumps/

# Service env with secrets
.env

# Case-specific legal filings (not part of the MCP source)
*.docx
ADA_access_request_OES.md
VDBC_registration_NLMA.md
